# Copy this file to ".env" and fill in your own values.
# This file contains placeholders; do not commit your real .env.

# OpenSky client credentials (optional but recommended)
OSK_CLIENT_ID=your_opensky_client_id
OSK_CLIENT_SECRET=your_opensky_client_secret

# FR24 options (optional, for the unofficial feed.js endpoint)
# Raw Cookie header value copied from your browser, if needed to bypass filtering
FR24_COOKIE=your_fr24_cookie_value
# Custom User-Agent for FR24 requests
FR24_UA=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124 Safari/537.36
# Mimic an ESP32 HTTP client (set to 1/true/yes/on to enable)
FR24_ESP=0

# MQTT (optional; used by scripts/publish_mqtt.sh)
# Point to your Home Assistant Mosquitto broker or external broker
MQTT_HOST=192.168.2.244
MQTT_PORT=1883
MQTT_USER=mqtt
MQTT_PASS=your_mqtt_password
MQTT_PREFIX=airtracker

# Optional: publish a device_tracker entity (map + history) for the nearest plane
# Set to 1/true to enable; 0/false to disable
MQTT_DEVICE_TRACKER=0

# Home Assistant MQTT Discovery (mqtt/producer/publish_ha_discovery.sh)
# Discovery prefix and device identity
HA_DISCOVERY_PREFIX=homeassistant
HA_DEVICE_ID=airtracker
HA_DEVICE_NAME=AirTracker
# Prune stale discovery topics after publishing (1/true to enable)
HA_DISCOVERY_PRUNE=0
# Print actions without changing the broker (1/true to enable)
HA_DISCOVERY_DRY_RUN=0
# Seconds to wait for retained messages when pruning (default 2)
HA_DISCOVERY_PRUNE_WAIT_SEC=2

# Default point and radius (optional; used by mqtt/producer/publish_mqtt.sh)
LAT=46.168689
LON=-123.020309
RADIUS_NM=25

# Publisher cadence (optional; used by mqtt/producer/publish_mqtt.sh)
# Option A: fixed fetch interval (seconds)
# FETCH_INTERVAL_SEC=90
# Option B: jittered fetch interval range (seconds)
FETCH_INTERVAL_MIN_SEC=80
FETCH_INTERVAL_MAX_SEC=100
# Publish intervals (defaults to FETCH_INTERVAL_MIN_SEC if unset)
# NEAREST_INTERVAL_SEC=80
# PLANES_INTERVAL_SEC=80

# Write merged JSON on each fetch (optional; relative to repo root if not absolute)
WRITE_JSON_PATH=./mqtt/producer/data/planes_merged.json

# Datasets path for merge enrichment (defaults to mqtt/producer/datasets)
MERGE_DATASETS=./mqtt/producer/datasets
